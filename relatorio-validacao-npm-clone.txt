RELATÓRIO — VALIDAÇÃO DO PROJETO COM NPM
Projeto: whatsapp-chip-maturator
Data: 2026-07-21

OBJETIVO
Validar o mesmo projeto fora do fluxo do `pnpm`, usando `npm` apenas como instrumento de diagnóstico, para responder:

1. a materialização dos módulos fica íntegra?
2. o `tsc` sai da fase de resolução de módulos?
3. os erros remanescentes são reais do projeto?

AMBIENTE DE TESTE

Clone usado:
- `c:\Users\AORUS\.trae\work\6a54f97e29ef92b9a21a9636\whatsapp-chip-maturator-npm-test`

O QUE FOI FEITO

1. criado clone limpo do projeto
2. removidos artefatos de lock do `pnpm`
3. executado:
   - `npm install`
4. validada resolução real:
   - `require.resolve('zod')`
   - `require.resolve('drizzle-orm')`
   - `require.resolve('@types/node/package.json')`
   - `require.resolve('vite/package.json')`
5. executado:
   - `npx tsc --noEmit`

RESULTADO DA MATERIALIZAÇÃO

`npm install` => OK

Resolução real de módulos => OK

Caminhos resolvidos:
- `zod` => `node_modules\\zod\\index.cjs`
- `drizzle-orm` => `node_modules\\drizzle-orm\\index.cjs`
- `@types/node` => `node_modules\\@types\\node\\package.json`
- `vite` => `node_modules\\vite\\package.json`

CONCLUSÃO PRINCIPAL

Com `npm`, a resolução de módulos volta a funcionar normalmente.

Isso confirma que:

- o problema de ambiente era do fluxo de materialização do `pnpm`
- a base do projeto pode ser analisada de forma útil quando os módulos são realmente instalados

CORREÇÕES ESTRUTURAIS APLICADAS DURANTE A VALIDAÇÃO

1. `tsconfig.json`
- `tsBuildInfoFile` saiu de `node_modules`
- `target` definido como `ES2022`
- `downlevelIteration: true`

2. `server/_core/index.ts`
- import de `ENV` adicionado

3. `server/infrastructure/queue/BullMQAdapter.ts`
- tipagem do adapter relaxada no boundary do BullMQ para remover incompatibilidades artificiais entre tipo concreto da lib e o contrato interno de adapter

EFEITO DESSAS CORREÇÕES

Os erros claramente introduzidos pelo D2 no topo da lista diminuíram:

- `TS2304` de `ENV` saiu
- `TS2322` do `BullMQAdapter` saiu

ESTADO ATUAL DO TSC NO CLONE COM NPM

O `tsc` ainda falha, mas agora por erros reais do projeto:

1. `TS2802`
- ocorre em:
  - `server/repositories/ledger/InMemoryBehaviorActionLedgerRepository.ts`
  - `server/repositories/observation/MemoryObservationRepository.ts`
  - `server/services/behavior/behaviorQueries.ts`

Observação:
- o `--showConfig` confirma `target: es2022` e `downlevelIteration: true`
- esse bloco merece investigação isolada, porque a configuração já está presente

2. `TS2345`
- `server/services/behavior/executionService.ts`
- incompatibilidade de tipos reais do domínio

3. `TS18047`
- `server/services/behaviorPlannerService.ts`
- possível nulo

4. `TS2339`
- `server/services/maturationEngine.ts`
- propriedade ausente em tipo inferido

INTERPRETAÇÃO

O bloqueio deixou de ser:
- resolução de módulos

E passou a ser:
- erros reais de tipagem/configuração do projeto

O QUE ISSO PERMITE CONCLUIR

1. o D2 não era a causa do colapso inicial
2. o `pnpm` estava contaminando a análise
3. com `npm`, a análise do TypeScript passa a fazer sentido
4. parte dos erros remanescentes é estrutural/legada, não necessariamente regressão do D2

VEREDITO

O experimento com `npm` cumpriu o objetivo:

- a resolução de módulos está comprovadamente saudável nesse caminho
- o `tsc` agora expõe erros reais do projeto
- as correções estruturais do D2 já reduziram o ruído introduzido pela própria rodada

PRÓXIMO PASSO CORRETO

Não voltar ao D2 ainda.

Antes disso, resolver ou classificar:

1. o bloco `TS2802`
2. os erros reais de tipo em:
   - `executionService.ts`
   - `behaviorPlannerService.ts`
   - `maturationEngine.ts`

Depois disso, faz sentido voltar para:
- testes
- build
- fechamento do D2
