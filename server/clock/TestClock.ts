import type { MutableClock } from "./MutableClock";

/**
 * Compatibilidade: nome antigo preservado.
 * Preferir `MutableClock` em documentação e código novo.
 */
export type TestClock = MutableClock;
